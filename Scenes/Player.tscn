[gd_scene load_steps=4 format=3 uid="uid://durf4fw0iv0bu"]

[ext_resource type="Texture2D" uid="uid://bksc56puj3rjm" path="res://Assets/image2.png" id="1_rpqav"]

[sub_resource type="GDScript" id="GDScript_kyqiw"]
resource_name = "Player "
script/source = "extends CharacterBody2D


const SPEED = 500.0
const JUMP_VELOCITY = -450.0
var jumped = 0


func _physics_process(delta):
	# Add the gravity.
	if not is_on_floor():
		velocity += get_gravity() * delta

	# Handle jump.
	if Input.is_action_just_pressed(\"ui_accept\") and (jumped < 2):
		velocity.y = JUMP_VELOCITY
		jumped += 1
	if is_on_floor():
		jumped = 0
	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	var direction = Input.get_axis(\"ui_left\", \"ui_right\")
	if direction:
		velocity.x = direction * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)

	move_and_slide()
"

[sub_resource type="CapsuleShape2D" id="CapsuleShape2D_kyqiw"]
radius = 48.0
height = 154.0

[node name="CharacterBody2D" type="CharacterBody2D"]
script = SubResource("GDScript_kyqiw")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
shape = SubResource("CapsuleShape2D_kyqiw")

[node name="Sprite2D" type="Sprite2D" parent="."]
position = Vector2(-1, -2)
scale = Vector2(0.3, 0.3)
texture = ExtResource("1_rpqav")

[node name="Camera2D" type="Camera2D" parent="Sprite2D"]
